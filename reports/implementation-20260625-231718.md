# AI Harness implementation report

Started: 2026-06-25T14:17:18Z
Workspace: /Users/nonaka/tasks/cross-agent-provider
Spec: /Users/nonaka/Downloads/AI_HARNESS_IMPLEMENTATION_SPEC_JA.md

## Initial environment

```text
Darwin nonakanoMac-mini.local 25.4.0 Darwin Kernel Version 25.4.0: Thu Mar 19 19:33:25 PDT 2026; root:xnu-12377.101.15~1/RELEASE_ARM64_T6041 arm64
SHELL=/opt/homebrew/bin/bash
claude=/Users/nonaka/.local/bin/claude
codex=/Users/nonaka/.agents/bin/codex
opencode=/Users/nonaka/.local/bin/opencode
claude_version=2.1.179 (Claude Code)
codex_version=codex-cli 0.142.0
opencode_version=local
```

## Backup

Created: `/Users/nonaka/.config/ai-harness/backups/20260625-231829`

Backed up existing config, shell rc files, current CLI shims, and LaunchAgent if present.

7.0G	/Users/nonaka/.config/ai-harness/backups/20260625-231829
Critical backup: `/Users/nonaka/.config/ai-harness/backups/critical-20260625-232016`

Note: large config backup `/Users/nonaka/.config/ai-harness/backups/20260625-231939` hit an rsync EOF on an active file and is treated as best-effort. Critical files modified by this run are separately backed up.

## CLIProxyAPI source inspection

Tag: `v7.2.39`
Source commit: `c4cf0fd3241afca8b63673bac7e0fc2e11ed9426`
Codex refresh: `internal/auth/codex/openai_auth.go` uses package-level `singleflight.Group`; `openai_auth_test.go` contains concurrent refresh test.

Release tarball SHA-256: `041dbb1e3549c2b9ac47f78ca29d8c4eb54da30777f8d14ee1c1e0528187652d`

## Runtime config

CLIProxyAPI runtime config generated at `/Users/nonaka/.config/ai-harness/cliproxy/config.yaml` (mode 600).
Management panel pinned at `/Users/nonaka/.local/share/ai-harness/cliproxy/static/management.html`.
Management panel SHA-256: `268bf8d53021bd3afbb695b2cabb9780c4a9546ffa70202517e79380cd4b12f3`

## Wrappers and PATH

Installed wrappers in `/Users/nonaka/.local/share/ai-harness/bin` and added shell source line to `.bashrc` and `.zshrc` if absent.

## Refresh tests

Ran `go test ./internal/auth/codex -run TestRefreshTokens... -count=1`: PASS.

## Consultation checkpoint

User requested consultation-first workflow. Reverted the unbuilt local source experiment; running CLIProxyAPI binary remains the pinned release.

## Codex streaming diagnostic

## Codex streaming diagnostic

- Pinned running binary: CLIProxyAPI v7.2.39 commit c4cf0fd3, SHA-256 `18666393fbd53895945fa3dd2f0dbe6c9074e5c37d2a813342a61f3be6766bca`.
- Source tree restored to clean `v7.2.39`; no unbuilt local patch remains.
- Non-stream `POST /v1/responses` with `oauth-gpt-5.5`: HTTP 200, JSON response, status completed.
- Stream `POST /v1/responses` with `stream:true`: HTTP 200, `Content-Type: text/event-stream`, body size 1 byte, no `response.completed`.
- Codex CLI 0.142.0 resolves model/provider/high correctly, then fails because the stream closes before `response.completed`.
- Oracle session `cliproxy-codex-stream-empty-browser2` agrees likely issue is CLIProxyAPI Codex streaming path/empty stream handling, not OAuth/model resolution.


## Streaming config retry

- Added config-only mitigation: `streaming.keepalive-seconds: 15`, `streaming.bootstrap-retries: 1` to runtime config and template. No request/debug logging enabled.

## Codex streaming repair

- Consulted Oracle session `cliproxy-codex-stream-translator-empty` after config-only mitigation failed.
- Temporarily enabled `request-log` for one minimal request, then disabled it again. Diagnostic log showed upstream Codex SSE completed, while downstream body was empty.
- Root cause isolated to Codex OAuth streaming path to OpenAI Responses clients: upstream `/backend-api/codex/responses` returned complete SSE, but downstream received only the handler's final newline.
- Applied local CLIProxyAPI source patch in `internal/runtime/executor/codex_executor.go` to frame Codex OpenAI Responses SSE as complete `event:`/`data:` frames for `openai-response` streaming clients.
- Added regression coverage in `internal/runtime/executor/codex_executor_stream_output_test.go` for event/data framing and large data lines.
- Passed targeted tests:
  - `go test ./internal/runtime/executor -run 'TestCodexExecutorExecuteStream' -count=1`
  - `go test ./sdk/api/handlers/openai -run 'TestForwardResponsesStream|TestResponsesSSE' -count=1`
- Broader `go test ./internal/runtime/executor ./sdk/api/handlers/openai ./sdk/api/handlers -count=1` still has an unrelated existing Antigravity reasoning replay failure: `TestPrepareAntigravityGeminiReasoningReplayPayloadAppendsStaleThoughtSignatureWithoutNullParts`.
- Built and installed local binary `7.2.39-ai-harness.1`, commit label `c4cf0fd3-local`, SHA-256 `a5c8f9b672836978cd0102afc914f479d7bb5e7ee5895f45bf44798c14fedbee`.
- Backed up pinned release binary SHA-256 `18666393fbd53895945fa3dd2f0dbe6c9074e5c37d2a813342a61f3be6766bca` as `/Users/nonaka/.local/libexec/ai-harness/cli-proxy-api.release-v7.2.39.20260626-002151.bak`.
- Live API stream check now returns SSE with `response.completed` instead of 1 byte.
- `codex exec --skip-git-repo-check -s read-only 'Return exactly OK-CODEX and do not run tools.'` succeeded with `OK-CODEX`.

- Removed temporary request-log file after summarizing diagnostic finding; runtime `request-log` remains disabled.

## Acceptance test completion

Created detailed report: `/Users/nonaka/.local/share/ai-harness/reports/TEST_REPORT.md`.

- `claude`, `claude-codex`, `claude-glm`, `codex`, `opencode-codex`, and `opencode-glm` passed smoke and tool-call edit tests.
- `codex-glm` remains intentionally gated pending explicit user acceptance of the Z.AI Coding Plan/Codex CLI compatibility risk.
- Fixed OpenCode wrapper duplicate `--variant high` handling.
- Fixed OpenCode parallel `database is locked` failure by using per-run `OPENCODE_DB` for noninteractive `run`.
- Final 10-request parallel test across `claude-codex`, `codex`, and `opencode-codex`: PASS, `ok_count=10`.
- CLIProxyAPI log scan after parallel test: no `refresh_token_reused`, `invalid_grant`, `panic`, or duplicate process errors.

## Deliverable bundle completion

Created `/Users/nonaka/.local/share/ai-harness/artifacts` with:

- `install.sh`, `rollback.sh`, `uninstall.sh`, `README.md`, `SECURITY_NOTES.md`, `ROLLBACK.md`, `TEST_REPORT.md`
- `wrappers/`
- `config/`
- `service/`
- `vendor/build-info.txt`
- `vendor/cliproxy-codex-openai-responses-sse.patch`
- `vendor/checksums.sha256`

Verified `bash -n` for scripts, `install.sh --dry-run`, and real `install.sh` idempotent check. `ai-auth status` remained OK after the check.

## Rollback hardening

Updated `ai-harness-rollback` to support `--dry-run`, generate a rollback report on real execution, remove harness shell source lines, unload the LaunchAgent, and quarantine wrappers, LaunchAgent plist, CLIProxyAPI binary, runtime config, auth store, dedicated harness settings, and harness Codex profile files without deleting credentials.

Verified:

- `bash -n ~/.local/share/ai-harness/bin/ai-harness-rollback ~/.local/share/ai-harness/rollback.sh ~/.local/share/ai-harness/uninstall.sh`: PASS
- `ai-harness-rollback --dry-run`: PASS
- `ai-auth status` after dry-run: PASS

## ai-auth hardening

Updated `ai-auth status` to include non-secret Codex OAuth metadata from Management API: status, updated time, success count, and failed count.

Updated `ai-auth logs` to print recent CLIProxyAPI error summaries only. Request/response bodies are omitted, and common token/header patterns are redacted.

Verified:

- `ai-auth status`: PASS
- `ai-auth logs`: PASS
- Secret-pattern scan of `ai-auth logs` output: PASS

## Maintenance dispatch and model identity evidence

- Claude noninteractive `/status`/`/model`/`/effort` probes return `/status isn't available in this environment`, so debug dispatch evidence is used instead.
- Claude `--debug-file` probes confirmed dispatch models:
  - `claude`: `claude-opus-4-8[1m]`
  - `claude-codex`: `oauth-gpt-5.5[1m]`
  - `claude-glm`: `glm-5.2[1m]`
- Fixed `codex` wrapper so `codex mcp list` is passed through without injecting proxy model profile; verified MCP list succeeds.
- Moved `codex-glm` maintenance passthrough before the acceptance gate so `codex-glm --help` works while `codex-glm exec` remains blocked pending explicit user acceptance.

## MCP and subagent route evidence

Added local `test-wait` MCP to the dedicated `claude-codex` and `claude-glm` config dirs using `claude mcp add`; no full user config copy was used.

Verified MCP tool invocation:

- `claude`: `mcp__test-wait__wait(seconds=0)` -> `MCP-WAIT-PASS`
- `claude-codex`: `mcp__test-wait__wait(seconds=0)` -> `MCP-WAIT-PASS`
- `claude-glm`: `mcp__test-wait__wait(seconds=0)` -> `MCP-WAIT-PASS`

Verified subagent invocation:

- `claude`: `general-purpose` subagent -> `SUBAGENT-OUTER-PASS`
- `claude-codex`: `general-purpose` subagent -> `SUBAGENT-OUTER-PASS`
- `claude-glm`: `general-purpose` subagent -> `SUBAGENT-OUTER-PASS`

Synced dedicated `.claude.json` files into `artifacts/config/claude-config-dirs`.
